/* @flow */
import React from 'react';

type Props = {

};

/**
 * The header for the site.
 */
export default function Header({ ...otherProps }: Props): React.Element<*> {
  return (
    <header {...otherProps}>
      NYI: Header
    </header>
  );
}
